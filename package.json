{
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prettier": "^3.2.5"
  },
  "scripts": {
    "test": "prettier --check . && npm run test:client && npm run test:server",
    "test:client": "cd client && npm test",
    "test:server": "cd server && npm test",
    "dev": "npm run dev:docker && concurrently npm:dev:client npm:dev:server",
    "dev:docker": "docker compose create && docker compose start",
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm run dev",
    "prepare": "husky"
  }
}
